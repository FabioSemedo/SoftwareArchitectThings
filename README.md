To run the scripts in [agent_latency](./agent_latency/)

In a terminal opened in this directory, run:
```bash
uv venv
source .venv/bin/activate
uv sync
```
This will allow you to use the modules in this repo.

To run `agent_latency/main.py`,
```bash
cd ./agent_latency
python main.py
```